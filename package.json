{
  "name": "lms-app",
  "version": "1.0.0",
  "description": "Learning Management System with React frontend and Node.js backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm start",
    "client": "cd frontend && npm start",
    "build": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["lms", "education", "react", "nodejs", "mongodb"],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
} 